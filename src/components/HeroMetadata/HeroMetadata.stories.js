import { HeroMetadata } from ".";

export default {
  title: "Components/HeroMetadata",
  component: HeroMetadata,
};

export const Default = {
  args: {
    className: {},
    runtimeOrSeasonsClassName: {},
  },
};
